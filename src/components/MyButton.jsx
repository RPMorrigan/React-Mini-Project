const MyButton = () => {
  const btnText = `Click Here to See the Creatures!`;
  return (
    <>
      <button onClick={() => }>
        {btnText}
        </button>
    </>
  );
};

export default MyButton;
